import { ColonizationViewer } from './colonization-viewer';

describe('ColonizationViewer', () => {
  it('should create an instance', () => {
    expect(new ColonizationViewer()).toBeTruthy();
  });
});
